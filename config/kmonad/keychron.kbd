(defcfg
    input (device-file "/dev/input/by-id/usb-Keychron_Keychron_Q1-if02-event-kbd")
    output (uinput-sink "keychron")
    fallthrough true
    allow-cmd false)

(defsrc
    esc
    caps)

(deflayer qwerty
    caps
    @capsesc)

(defalias
    capsesc (tap-hold-next-release 200 esc lctl))
