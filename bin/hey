#! /usr/bin/env cached-nix-shell
#! nix-shell -i python3 -p nixFlakes python310 python310Packages.click fzf
# vim: ft=python

# yo command
# use this when you have finished the install
# cached-nix-shell will be available which is much faster

import hey_internal

if __name__ == '__main__':
    hey_internal.main()
